# ── Required environment variables ───────────────────────────
# Copy this file to .env.production and fill in your values
# Then add each key to Vercel → Project → Settings → Environment Variables

# Laravel API URL (your backend deployment URL)
NG_APP_API_URL=https://api.yourfarm.com/api

# Environment label
NG_APP_ENV=production
